You are onboarding into this repository.

Do the following, in order:
1) READ `CLAUDE.md` + `README.md` and summarize the project in 10 bullets max.
2) Inspect repo structure (list top-level folders) and infer where:
   - Next.js app lives
   - Strapi app lives
3) Identify how to run frontend and backend (from package.json scripts).
4) Output:
   - “Current goal” (1–2 sentences)
   - “How to run locally” (exact commands)
   - “Key constraints” (security, style, conventions)
   - “Next 3 actions” (short, concrete)
Keep it concise and operational.
